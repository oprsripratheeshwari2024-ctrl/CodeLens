# Copy this file to backend/.env and fill in your key.
# CodeLens works without this key too — it automatically falls back
# to template-based explanations when no key is configured.

OPENAI_API_KEY=your_api_key_here
