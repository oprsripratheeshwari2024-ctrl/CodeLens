# CodeLens Frontend

React + Vite + Tailwind CSS, Monaco Editor, React Flow, Recharts.

```powershell
npm install
npm run dev
```

Runs at http://localhost:3000 and expects the backend at http://localhost:8000 (see `src/services/api.js`).

Build for production with `npm run build` (outputs to `dist/`).
